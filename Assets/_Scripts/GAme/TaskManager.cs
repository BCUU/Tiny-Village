using System.Collections.Generic;
using UnityEngine;

public class TaskManager : MonoBehaviour
{
    //public List<Task> availableTasks = new List<Task>(); // Görev listesi
    //public List<Villager> villagers = new List<Villager>(); // Köylü listesi

    //void Start()
    //{
    //    // Örnek görevler ekleyelim
    //    availableTasks.Add(new Task(TaskType.ChopWood, GameObject.Find("Tree").transform));
    //    //availableTasks.Add(new Task(TaskType.Mine, GameObject.Find("Mine").transform));

    //    AssignTasks();
    //}

    //void AssignTasks()
    //{
    //    foreach (var villager in villagers)
    //    {
    //        if (villager.currentTask == null && availableTasks.Count > 0)
    //        {
    //            Task task = availableTasks[0];
    //            availableTasks.RemoveAt(0);
    //            villager.AssignTask(task);
    //        }
    //    }
    //}
}
